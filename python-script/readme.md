## Python Script for carla

#### Getting Started
Start a carla server and then run the script:
```sh
uv run main.py
```

Run the application with `sudo` privileges:
```sh
uv venv
sudo .venv/bin/python main.py
```
